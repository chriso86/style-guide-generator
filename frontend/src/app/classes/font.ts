export class Font {
  label: string;
  name: string;
  description: string;

  constructor(label: string, name: string, description: string) {
    this.label = label;
    this.name = name;
    this.description = description;
  }
}
